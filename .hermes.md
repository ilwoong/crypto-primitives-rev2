# Hermes — crypto-primitives-rev2

이 저장소는 Hermes에서도 사용할 수 있습니다. Hermes는 이 `.hermes.md`를 먼저
읽으므로, 오케스트레이터는 루트의 `AGENTS.md`(cwd에서만 읽힘)와 `README.md`도
반드시 직접 읽어야 합니다.
`CLAUDE.md`는 `@AGENTS.md` import만 하므로 Claude Code 전용 파일에 의존하지 않습니다.

## 에이전트 래퍼

도구별 서브에이전트 래퍼가 병렬로 존재합니다. 모두 도구 중립 역할 지시문인
`docs/agents/<role>.md`를 가리키며, 각 도구 전용 설정은 래퍼 파일 자체에만 둡니다.

- `.claude/agents/<role>.md` — Claude Code용
- `.codex/agents/<role>.toml` — Codex용
- `.hermes/agents/<role>.md` — Hermes용 (이 디렉터리)

역할( coder, formatter, reviewer, tester, fixer, documenter )과 권한,
파이프라인 순서는 `docs/agents/README.md`와 `AGENTS.md`의 "에이전트 파이프라인"을
따릅니다.

## Hermes에서 파이프라인을 돌리는 방법

Hermes 세션에서 에이전트 파이프라인을 재현하려면 두 가지 방식이 있습니다.

1. **이 세션이 오케스트레이터 역할** — `AGENTS.md`, `README.md`, 각 단계의 역할
   지시문(`docs/agents/<role>.md`)을 순서대로 읽은 뒤 `delegate_task`로 하위 단계를
   위임합니다. `.hermes/agents/*.md`는 Hermes가 자동으로 검색하거나 실행 권한·모델을
   강제하는 설정이 아니라 안내 래퍼입니다. 따라서 `delegate_task` 목표/컨텍스트에
   역할의 쓰기 범위와 마무리 보고 형식을 명시하세요. 제한 역할 전후에는 AGENTS.md의
   상태 비교 규칙으로 허용되지 않은 파일 변경이 없음을 검증하세요. `delegate_task`는
   역할별 `model:` 값을 적용하지 않으므로 전역 모델 정책을 사용합니다.
2. **별도 hermes 프로세스를 역할별로 spawn** — `delegate_task` 대신 tmux 등으로
   독립적인 Hermes 프로세스를 띄우고 각 역할에 맞는 목표를 줍니다. 역할별 모델이
   필요하면 이 방식에서 `hermes chat --oneshot -m <model> --in <repo>`를 사용합니다.
   장시간/자율 작업에 적합합니다.

어느 방식이든 `docs/agents/<role>.md`를 먼저 읽고 그 지시를 따르게 하세요.
래퍼 파일(`.hermes/agents/<role>.md`)은 해당 역할의 간단한 안내와 Hermes 위임 시
유의점만을 담고 있습니다.
